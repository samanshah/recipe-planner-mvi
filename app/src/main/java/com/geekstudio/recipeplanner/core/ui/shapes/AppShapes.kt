package com.geekstudio.recipeplanner.core.ui.shapes

import androidx.compose.foundation.shape.RoundedCornerShape
import com.geekstudio.recipeplanner.core.ui.spacing.AppSpacing

object AppShapes {

    val Card = RoundedCornerShape(
        AppSpacing.CardCornerRadius
    )

    val Button = RoundedCornerShape(
        AppSpacing.ButtonCornerRadius
    )

}